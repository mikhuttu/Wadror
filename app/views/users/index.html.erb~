<p id="notice"><%= notice %></p>

<h1>Listing Users</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Username</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user.username, user %></td>
        
        <% if admin and not user.active %>
          <td><button type="button" class="btn btn-primary" class="disabled">account frozen</button></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
